# Локальная разработка edge functions: supabase start + supabase functions serve
# Деплой: supabase functions deploy ai-rewrite --project-ref <ref>

# sb_publishable_* в apikey: platform verify_jwt ожидает legacy anon JWT.
# Авторизацию делаем внутри функции (/auth/v1/user + getUser).
[functions.ai-rewrite]
verify_jwt = false
